{% extends "MlGroupBundle::layout.html.twig" %}

{% block title %}
 {{ parent() }} - Création - Groupe
{% endblock %}

{% block group_body %}

	{% if error is defined %}
		<h5 style="color:red">{{ error }}</h5>
	{% endif %}

	<table class="table">
		<legend><b>Créer votre groupe</b></legend>
		<tr>
			<td>
				<div class="well">
					<form method="post" class="creation_group" {{ form_enctype(form) }}>
							{{ form_label(form.name) }}
							{{ form_errors(form.name) }}
							{{ form_widget(form.name) }}
							{{ form_label(form.description) }}
							{{ form_errors(form.description) }}
							{{ form_widget(form.description) }}
				</div>
			</td>
		</tr>
		{% if form.users is defined %}
			<tr>
				<td>
					<div class="well">
						{{ form_label(form.users) }}
						{{ form_errors(form.users) }}
						{{ form_widget(form.users) }}
					</div>
				</td>
			</tr>
		{% endif %}
	</table>
	<button id="form_validatation_group" class="btn btn-primary btn-xs" type="submit">Créer <i class="icon-white glyphicon glyphicon-ok-sign"></i> </button>
	</form>

{% endblock %}